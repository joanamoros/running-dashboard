// src/pages/Races.tsx
import { FaStrava } from "react-icons/fa";
import { useMemo } from "react";

interface Race {
  name: string;
  distance: string;
  distance_km: number;
  location: string;
  date: string;
  date_obj: Date;
  dorsal?: number | string;
  finish_time: string;
  official_time?: string;
  pace: string;
  general_position?: string;
  category_position?: string;
  color_bg: string;
  color_text: string;
  split_5k?: string;
  split_4k?: string;
}

// Datos actualizados según tu tabla
const races: Race[] = [
  {
    name: "XXVI Carrera al Amanecer Santa Pola",
    distance: "6 km",
    distance_km: 6,
    location: "Santa Pola",
    date: "2022-08-28",
    date_obj: new Date("2022-08-28"),
    dorsal: 723,
    finish_time: "30:16",
    official_time: "30:42",
    pace: "5'07/km",
    general_position: "444/1411",
    category_position: "393/971",
    color_bg: "#2596be",
    color_text: "white",
  },
  {
    name: "VIII 10k Rotary Elche",
    distance: "10 km",
    distance_km: 10,
    location: "Elche",
    date: "2023-02-12",
    date_obj: new Date("2023-02-12"),
    dorsal: 706,
    finish_time: "52:44",
    official_time: "52:56",
    pace: "5'18/km",
    general_position: "522/784",
    category_position: "201/242",
    color_bg: "#faae3f",
    color_text: "#2e56a3",
  },
  {
    name: "Benidorm Half (10k)",
    distance: "10 km",
    distance_km: 10,
    location: "Benidorm",
    date: "2023-11-25",
    date_obj: new Date("2023-11-25"),
    dorsal: 4396,
    finish_time: "49:09",
    official_time: "49:24",
    pace: "4'56/km",
    general_position: "453/2059",
    category_position: "12/21",
    color_bg: "#ffffff",
    color_text: "#82afcd",
  },
  {
    name: "III Elche Night Race (5k)",
    distance: "5 km",
    distance_km: 5,
    location: "Elche",
    date: "2024-05-11",
    date_obj: new Date("2024-05-11"),
    dorsal: 3061,
    finish_time: "28:27",
    official_time: "29:04",
    pace: "5'49/km",
    general_position: "358/986",
    category_position: "135/226",
    color_bg: "#000000",
    color_text: "#59d14b",
  },
  {
    name: "52 Media Elche (10k Pompadour)",
    distance: "10 km",
    distance_km: 10,
    location: "Elche",
    date: "2025-03-23",
    date_obj: new Date("2025-03-23"),
    dorsal: 10546,
    finish_time: "48:55",
    official_time: "49:49",
    pace: "4'59/km",
    split_5k: "25:14",
    general_position: "317/1292",
    category_position: "115/280",
    color_bg: "#e20909",
    color_text: "#ffffff",
  },
  {
    name: "IV Elche Night Race (5k)",
    distance: "5 km",
    distance_km: 5,
    location: "Elche",
    date: "2025-05-10",
    date_obj: new Date("2025-05-10"),
    dorsal: 3222,
    finish_time: "24:19",
    official_time: "24:39",
    pace: "4'56/km",
    general_position: "221/1265",
    category_position: "42/89",
    color_bg: "#000000",
    color_text: "#59d14b",
  },
  {
    name: "XXIX Carrera al Amanecer Santa Pola",
    distance: "6 km",
    distance_km: 6,
    location: "Santa Pola",
    date: "2025-08-31",
    date_obj: new Date("2025-08-31"),
    dorsal: 1096,
    finish_time: "26:14",
    official_time: "26:22",
    pace: "4'24/km",
    general_position: "140/1806",
    category_position: "126/1073",
    color_bg: "#2596be",
    color_text: "white",
  },
  {
    name: "10k Internacional Santa Pola",
    distance: "10 km",
    distance_km: 10,
    location: "Santa Pola",
    date: "2026-01-18",
    date_obj: new Date("2026-01-18"),
    dorsal: 7262,
    finish_time: "44:17",
    official_time: "44:23",
    pace: "4'26/km",
    color_bg: "#f4b00e",
    color_text: "#000000",
  },
];

// Formatear fecha
const formatDate = (dateStr: string): string => {
  const date = new Date(dateStr);
  return date.toLocaleDateString('es-ES', { 
    day: 'numeric', 
    month: 'short', 
    year: 'numeric' 
  }).replace(/ /g, ' ');
};

export default function Races() {
  // Ordenar carreras por fecha (más reciente a más antigua)
  const sortedRaces = useMemo(() => {
    return [...races].sort((a, b) => b.date_obj.getTime() - a.date_obj.getTime());
  }, []);

  // Estadísticas actualizadas
  const stats = useMemo(() => {
    const total = sortedRaces.length;
    const cincoK = sortedRaces.filter(r => r.distance_km === 5).length;
    const seisK = sortedRaces.filter(r => r.distance_km === 6).length;
    const diezK = sortedRaces.filter(r => r.distance_km === 10).length;
    const mediaMaraton = sortedRaces.filter(r => r.distance_km === 21.1).length;

    return { total, cincoK, seisK, diezK, mediaMaraton };
  }, [sortedRaces]);

  // Mejor tiempo en 5k
  const best5k = useMemo(() => {
    const cincoK = races.filter(r => r.distance_km === 5);
    if (cincoK.length === 0) return null;
    return cincoK.reduce((best, current) => {
      if (!best) return current;
      return current.finish_time < best.finish_time ? current : best;
    }, null as Race | null);
  }, []);

  // Mejor tiempo en 10k
  const best10k = useMemo(() => {
    const diezK = races.filter(r => r.distance_km === 10);
    if (diezK.length === 0) return null;
    return diezK.reduce((best, current) => {
      if (!best) return current;
      return current.finish_time < best.finish_time ? current : best;
    }, null as Race | null);
  }, []);

  return (
    <>
      <section id="hero">
        <h1>
          <span className="gradient-text">Carreras</span>
        </h1>
        <p>Un recorrido por mis logros y experiencias en el mundo del running</p>
        <a
          href="https://www.strava.com/athletes/74996691"
          target="_blank"
          rel="noopener noreferrer"
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: '0.5rem',
            padding: '0.75rem 1.5rem',
            background: '#fc4c02',
            color: 'white',
            borderRadius: '9999px',
            textDecoration: 'none',
            fontWeight: '600',
            marginTop: '1rem'
          }}
        >
          <FaStrava size={20} />
          Ver perfil en Strava
        </a>
      </section>

      {/* Resumen de estadísticas */}
      <section className="races-container" style={{ paddingTop: 0 }}>
        <div className="stats-grid" style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))',
          gap: 'var(--space-lg)',
          marginBottom: 'var(--space-2xl)'
        }}>
          <div className="stat-card" style={{
            padding: 'var(--space-lg)',
            borderRadius: 'var(--radius-lg)',
            background: 'var(--light-card-bg)',
            backdropFilter: 'blur(10px)',
            border: '1px solid var(--light-nav-border)',
            textAlign: 'center'
          }}>
            <div style={{ fontSize: '2rem', fontWeight: '700' }}>{stats.total}</div>
            <div style={{ fontSize: '0.9rem', opacity: 0.8 }}>Total carreras</div>
          </div>
          
          <div className="stat-card" style={{
            padding: 'var(--space-lg)',
            borderRadius: 'var(--radius-lg)',
            background: 'linear-gradient(135deg, #667eea, #764ba2)',
            color: 'white',
            textAlign: 'center'
          }}>
            <div style={{ fontSize: '2rem', fontWeight: '700' }}>{stats.diezK}</div>
            <div style={{ fontSize: '0.9rem', opacity: 0.9 }}>10 km</div>
            {best10k && <div style={{ fontSize: '0.8rem', marginTop: '0.5rem' }}>Mejor: {best10k.finish_time}</div>}
          </div>
          
          <div className="stat-card" style={{
            padding: 'var(--space-lg)',
            borderRadius: 'var(--radius-lg)',
            background: 'linear-gradient(135deg, #ff6b6b, #ee5a24)',
            color: 'white',
            textAlign: 'center'
          }}>
            <div style={{ fontSize: '2rem', fontWeight: '700' }}>{stats.cincoK}</div>
            <div style={{ fontSize: '0.9rem', opacity: 0.9 }}>5 km</div>
            {best5k && <div style={{ fontSize: '0.8rem', marginTop: '0.5rem' }}>Mejor: {best5k.finish_time}</div>}
          </div>
          
          <div className="stat-card" style={{
            padding: 'var(--space-lg)',
            borderRadius: 'var(--radius-lg)',
            background: 'linear-gradient(135deg, #3498db, #2980b9)',
            color: 'white',
            textAlign: 'center'
          }}>
            <div style={{ fontSize: '2rem', fontWeight: '700' }}>{stats.seisK}</div>
            <div style={{ fontSize: '0.9rem', opacity: 0.9 }}>6 km</div>
          </div>
        </div>

        {/* Lista de carreras */}
        <div className="races-timeline">
          {sortedRaces.map((race, index) => (
            <div
              key={`${race.name}-${race.date}`}
              className="race-card"
              style={{
                borderRadius: 'var(--radius-lg)',
                marginBottom: 'var(--space-md)',
                padding: 'var(--space-lg)',
                background: 'var(--light-card-bg)',
                backdropFilter: 'blur(10px)',
                border: '1px solid var(--light-nav-border)',
                borderLeft: `4px solid ${race.color_bg}`,
                transition: 'all 0.3s ease'
              }}
            >
              <div style={{
                display: 'flex',
                flexWrap: 'wrap',
                gap: 'var(--space-lg)',
                alignItems: 'flex-start'
              }}>
                {/* Fecha destacada */}
                <div style={{
                  minWidth: '100px',
                  textAlign: 'center'
                }}>
                  <div style={{
                    fontSize: '1.5rem',
                    fontWeight: '700',
                    color: race.color_bg
                  }}>
                    {new Date(race.date).getFullYear()}
                  </div>
                  <div style={{
                    fontSize: '0.85rem',
                    opacity: 0.7
                  }}>
                    {formatDate(race.date)}
                  </div>
                </div>

                {/* Información principal */}
                <div style={{ flex: 1 }}>
                  <h3 style={{
                    fontSize: '1.25rem',
                    fontWeight: '600',
                    marginBottom: 'var(--space-xs)',
                    color: 'inherit'
                  }}>
                    {race.name}
                  </h3>
                  
                  <div style={{
                    display: 'flex',
                    flexWrap: 'wrap',
                    gap: 'var(--space-md)',
                    marginBottom: 'var(--space-sm)',
                    fontSize: '0.95rem',
                    fontWeight: '500'
                  }}>
                    <span>{race.location}</span>
                    <span>• {race.distance}</span>
                    {race.dorsal && <span>• #{race.dorsal}</span>}
                  </div>

                  {/* Tiempos y ritmo */}
                  <div style={{
                    display: 'flex',
                    flexWrap: 'wrap',
                    gap: 'var(--space-xl)',
                    marginTop: 'var(--space-sm)'
                  }}>
                    <div>
                      <div style={{ fontSize: '0.75rem', opacity: 0.6 }}>TIEMPO</div>
                      <div style={{ fontSize: '1.5rem', fontWeight: '700' }}>
                        {race.finish_time}
                      </div>
                      {race.official_time && (
                        <div style={{ fontSize: '0.85rem', opacity: 0.6 }}>
                          oficial: {race.official_time}
                        </div>
                      )}
                    </div>

                    <div>
                      <div style={{ fontSize: '0.75rem', opacity: 0.6 }}>RITMO</div>
                      <div style={{ fontSize: '1.25rem', fontWeight: '600' }}>
                        {race.pace}
                      </div>
                    </div>

                    {race.general_position && (
                      <div>
                        <div style={{ fontSize: '0.75rem', opacity: 0.6 }}>POSICIÓN</div>
                        <div style={{ fontSize: '1rem', fontWeight: '500' }}>
                          {race.general_position}
                        </div>
                        {race.category_position && (
                          <div style={{ fontSize: '0.85rem', opacity: 0.6 }}>
                            Cat: {race.category_position}
                          </div>
                        )}
                      </div>
                    )}
                  </div>

                  {/* Splits */}
                  {race.split_5k && (
                    <div style={{
                      marginTop: 'var(--space-md)'
                    }}>
                      <span style={{
                        padding: '0.25rem 0.75rem',
                        background: 'rgba(0,0,0,0.1)',
                        borderRadius: '9999px',
                        fontSize: '0.85rem'
                      }}>
                        Paso 5k: {race.split_5k}
                      </span>
                    </div>
                  )}
                </div>
              </div>
            </div>
          ))}
        </div>
      </section>
    </>
  );
}


----------------------------------------------------------------------

CSS:

/* ===== RACES SECTION - DISEÑO EN FILAS ===== */
.races-container {
  max-width: 1000px;
  margin: 0 auto;
  padding: var(--space-2xl) var(--space-lg);
  display: flex;
  flex-direction: column;
  gap: var(--space-xl);
}

.race-row {
  border-radius: var(--radius-xl);
  padding: var(--space-xl);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(10px);
  border-left: 6px solid;
}

.race-row::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: linear-gradient(90deg, transparent, rgba(168, 156, 156, 0.5), transparent);
}

.race-row:hover {
  transform: translateY(-4px) scale(1.01);
  box-shadow: var(--shadow-hover);
}

.race-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: var(--space-lg);
  gap: var(--space-md);
}

.race-header h3 {
  font-size: 1.5rem;
  font-weight: 700;
  margin: 0;
  flex: 1;
  line-height: 1.2;
}

.race-meta {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: var(--space-xs);
  font-size: 0.9rem;
  opacity: 0.9;
  text-align: right;
}

.race-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: var(--space-xl);
  margin-bottom: var(--space-lg);
}

.race-info {
  display: flex;
  align-items: center;
  gap: var(--space-xl);
  flex: 1;
}

.race-editions {
  display: flex;
  flex-direction: column;
  gap: var(--space-lg);
  margin-top: var(--space-md);
}

.race-edition {
  padding: var(--space-lg);
  background: rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-lg);
  backdrop-filter: blur(10px);
}

.edition-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-md);
  padding-bottom: var(--space-sm);
  border-bottom: 1px solid rgb(0, 0, 0);
}

.edition-year {
  font-size: 1.1rem;
  font-weight: 700;
  padding: var(--space-xs) var(--space-md);
  background: rgba(255, 255, 255, 0.2);
  border-radius: var(--radius-full);
}

.edition-distance {
  font-size: 0.95rem;
  font-weight: 600;
  opacity: 0.9;
}

.race-info-row {
  display: flex;
  align-items: center;
  gap: var(--space-lg);
  flex-wrap: wrap;
  padding: var(--space-md) 0;
}

.info-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-xs);
  text-align: center;
  min-width: 80px;
}

.info-label {
  font-size: 0.75rem;
  opacity: 0.8;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-weight: 600;
}

.info-value {
  font-size: 0.95rem;
  font-weight: 600;
  letter-spacing: -0.025em;
}

.info-item.year-badge {
  margin-left: auto;
  padding: var(--space-xs) var(--space-md);
  background: rgba(255, 255, 255, 0.2);
  border-radius: var(--radius-full);
  backdrop-filter: blur(10px);
}

.info-item.year-badge .info-value {
  font-size: 0.85rem;
  font-weight: 700;
  opacity: 0.9;
}

.edition-footer {
  margin-top: var(--space-sm);
  padding-top: var(--space-sm);
  text-align: center;
}

.dorsal-info {
  font-size: 0.85rem;
  opacity: 0.8;
  font-weight: 500;
}

.distance {
  font-size: 1.1rem;
  font-weight: 700;
  padding: var(--space-sm) var(--space-lg);
  border-radius: var(--radius-full);
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3);
}

.race-stats {
  display: flex;
  gap: var(--space-xl);
  flex: 1;
}

.stat {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-xs);
  text-align: center;
}

.stat-label {
  font-size: 0.8rem;
  opacity: 0.8;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-weight: 600;
}

.stat-value {
  font-size: 1.25rem;
  font-weight: 700;
  letter-spacing: -0.025em;
}

.split, .category {
  font-size: 0.9rem;
  opacity: 0.9;
  font-weight: 500;
}

.race-badges {
  display: flex;
  gap: var(--space-sm);
  flex-wrap: wrap;
  justify-content: flex-end;
}

.badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
  padding: var(--space-sm) var(--space-md);
  border-radius: var(--radius-full);
  font-size: 0.8rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3);
}

.badge.finisher {
  background: linear-gradient(135deg, #FFD700, #FFA500);
  color: #000;
}

.badge.personal-record {
  background: linear-gradient(135deg, #FF6B6B, #EE5A24);
  color: white;
}

.badge.podium {
  background: linear-gradient(135deg, #C0C0C0, #A0A0A0);
  color: white;
}

.edition-selector {
  display: flex;
  gap: var(--space-sm);
  flex-wrap: wrap;
  justify-content: center;
  padding-top: var(--space-md);
  border-top: 1px solid rgba(255, 255, 255, 0.2);
}

.edition-btn {
  border: 2px solid currentColor;
  background: transparent;
  padding: var(--space-sm) var(--space-md);
  border-radius: var(--radius-md);
  cursor: pointer;
  font-size: 0.85rem;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  font-weight: 600;
  letter-spacing: 0.025em;
}

.edition-btn:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-medium);
}

.edition-btn.active {
  font-weight: 700;
  box-shadow: var(--shadow-heavy);
  transform: translateY(-1px);
}


Responsive (768px): 

.races-container {
    padding: var(--space-xl) var(--space-md);
    gap: var(--space-lg);
  }
  
  .race-header {
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-sm);
  }
  
  .race-meta {
    align-items: flex-start;
    text-align: left;
  }
  
  .race-content {
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-lg);
  }
  
  .race-info {
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-lg);
    width: 100%;
  }

   .race-edition {
    padding: var(--space-md);
  }
  
  .edition-header {
    flex-direction: column;
    gap: var(--space-sm);
    align-items: flex-start;
  }
  
  .race-info-row {
    gap: var(--space-md);
    justify-content: space-between;
  }
  
  .info-item {
    min-width: 70px;
    flex: 1;
  }
  
  .info-item.year-badge {
    margin-left: 0;
    margin-top: var(--space-sm);
    order: 999;
    flex-basis: 100%;
    justify-content: center;
  }
  
  .race-stats {
    width: 100%;
    justify-content: space-between;
    gap: var(--space-md);
  }
  
  .race-badges {
    width: 100%;
    justify-content: flex-start;
  }
  
  .edition-selector {
    justify-content: flex-start;
  }

Responsive (480px): 
.race-stats {
    flex-wrap: wrap;
    gap: var(--space-md);
  }

.race-row {
    padding: var(--space-lg);
  }

  .race-info-row {
    gap: var(--space-sm);
  }


